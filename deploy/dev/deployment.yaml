apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicebus-ingester-go
  namespace: experiment
  labels:
    app: servicebus-ingester-go
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 100%
      maxSurge: 100%
  replicas: 2
  selector:
    matchLabels:
      app: servicebus-ingester-go
  template:
    metadata:
      labels:
        app: servicebus-ingester-go
        app.kubernetes.io/name: servicebus-ingester-go
        app.kubernetes.io/component: server
        log-source: dcsp-backend
        team: platform
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        pool_type: general
      tolerations:
        - key: "kubernetes.azure.com/scalesetpriority"
          operator: "Equal"
          value: "spot"
          effect: "NoSchedule"
      containers:
        - name: ingester
          image: acrvcedcsp.azurecr.io/experiment/ingestion/servicebus-ingester-go:20260319.141747
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: global-env-config
            - configMapRef:
                name: servicebus-ingester-go-config
            - secretRef:
                name: servicebus-ingester-go-kv-experiment-config
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 768m
              memory: 768Mi
            requests:
              cpu: 50m
              memory: 256Mi
